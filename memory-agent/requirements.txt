# Memory Agent Dependencies - Python 3.10+

# Core
python-dotenv

# Embeddings / Vector search
sentence-transformers>=2.2.0
numpy>=1.24.0

# Vector store - Pinecone (primary)
pinecone

# Optional vector store (enabled automatically if installed)
# chromadb>=0.4.0

# RAG document processing
pdfminer.six>=20221105
beautifulsoup4>=4.12.0

# LLM integration (arbiter)
openai

# STM backend
redis>=4.5.0
